#!/usr/bin/env sh

DIR=$HOME/.opt/bin
FILE=$DIR/nvim

if [ -f "$FILE" ];
then
    cp $FILE "${FILE}.old"
fi

curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.appimage
chmod +x nvim-linux-x86_64.appimage
mkdir -p $DIR
mv nvim-linux-x86_64.appimage $FILE
